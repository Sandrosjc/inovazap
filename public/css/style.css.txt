* { margin: 0; padding: 0; box-sizing: border-box; font-family: Arial, sans-serif; }

body { background: #f4f7f6; color: #1f2937; line-height: 1.6; }

.navbar {
  background: #075e54; color: white;
  display: flex; justify-content: space-between; align-items: center;
  padding: 1rem 2rem;
}
.logo { font-size: 1.4rem; font-weight: bold; }
.navbar nav a { color: white; text-decoration: none; margin-left: 20px; opacity: 0.85; }

.hero {
  padding: 3rem 2rem; text-align: center;
  background: linear-gradient(135deg, #075e54, #128c7e);
  color: white;
}
.hero h1 { font-size: 2rem; margin-bottom: 0.5rem; }

.motor-section, .custom-section {
  max-width: 900px; margin: 3rem auto; padding: 0 1.5rem;
}
.motor-section h2, .custom-section h2 { color: #075e54; margin-bottom: 0.5rem; }
.subtitle { color: #6b7280; margin-bottom: 1.5rem; }

.search-box { position: relative; }
.search-box input {
  width: 100%; padding: 1rem; border: 2px solid #e5e7eb;
  border-radius: 12px; font-size: 1rem; outline: none;
}
.search-box input:focus { border-color: #25d366; }

.autocomplete {
  position: absolute; top: 100%; left: 0; right: 0;
  background: white; border-radius: 12px;
  box-shadow: 0 8px 24px rgba(0,0,0,0.12);
  max-height: 300px; overflow-y: auto;
  display: none; margin-top: 4px; z-index: 50;
}
.autocomplete.open { display: block; }
.autocomplete-item {
  padding: 0.85rem 1.2rem; cursor: pointer;
  display: flex; justify-content: space-between;
  border-bottom: 1px solid #e5e7eb;
}
.autocomplete-item:hover { background: #f0fdf4; }
.autocomplete-item .cat { color: #6b7280; font-size: 0.85rem; }

.category-chips { display: flex; flex-wrap: wrap; gap: 8px; margin: 1.5rem 0; }
.chip {
  background: white; border: 1px solid #e5e7eb;
  border-radius: 999px; padding: 0.5rem 1rem;
  font-size: 0.9rem; cursor: pointer;
}
.chip:hover { background: #075e54; color: white; }

.unlock-result { margin-top: 2rem; }
.result-card {
  background: white; border-radius: 16px; padding: 2rem;
  box-shadow: 0 2px 8px rgba(0,0,0,0.06);
  border-left: 5px solid #25d366;
}
.result-card h3 { color: #075e54; font-size: 1.4rem; margin-bottom: 0.5rem; }
.result-card .meta { color: #6b7280; font-size: 0.9rem; margin-bottom: 1.5rem; }
.result-section { margin-top: 1.5rem; }
.result-section h4 { color: #128c7e; margin-bottom: 0.75rem; text-transform: uppercase; font-size: 0.95rem; }

.paradigm-item, .workflow-item {
  background: #f9fafb; border-radius: 10px;
  padding: 1rem; margin-bottom: 0.75rem;
  border-left: 3px solid #25d366;
}
.paradigm-item strong, .workflow-item strong { display: block; margin-bottom: 0.5rem; }
.paradigm-item ul { list-style: none; }
.paradigm-item li { padding: 0.2rem 0; font-size: 0.92rem; }
.workflow-item .tag {
  display: inline-block; background: #075e54; color: white;
  font-size: 0.75rem; padding: 0.15rem 0.6rem;
  border-radius: 999px; margin-right: 6px;
}

.custom-form {
  background: white; padding: 2rem; border-radius: 16px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.06);
  display: flex; flex-direction: column; gap: 1rem;
}
.custom-form input, .custom-form textarea {
  padding: 0.9rem; border: 2px solid #e5e7eb;
  border-radius: 10px; font-size: 0.95rem; outline: none;
}
.custom-form input:focus, .custom-form textarea:focus { border-color: #25d366; }
.custom-form textarea { min-height: 80px; }
.custom-form button {
  background: #25d366; color: white; border: none;
  padding: 1rem; border-radius: 10px;
  font-size: 1rem; font-weight: bold; cursor: pointer;
}
.custom-form button:hover { background: #1eaa54; }

.footer {
  background: #075e54; color: white; text-align: center;
  padding: 1.5rem; font-size: 0.9rem; margin-top: 3rem;
}
.footer .version { opacity: 0.75; font-size: 0.8rem; margin-top: 0.25rem; }